#include <stdio.h>
/**
 * main - entry point
 *
 * Return: always 0 (success)
 */
int main(void){
int i;
printf("base digits 10:");
for(i = 0; i < 10; i++){
printf("%d ", i);
}
printf("\n");
return (0);
}
