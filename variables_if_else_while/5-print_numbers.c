#include <stdio.h>
/**
 * main - entry point
 *
 * Return: always 0 (success)
 */
int main(void)
{
int i;
char digits[] = "0123456789";

printf("base digits 10:\n");
printf("%s\n", digits);

return (0);
}
